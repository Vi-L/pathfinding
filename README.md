console logs show that last element gets popped but openSet length never goes to 1
so that last element gets put through infinitely

heap:
- last pop not removing last element? can check testHeapSort 